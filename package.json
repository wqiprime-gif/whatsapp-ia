{
  "name": "whatsapp-ia",
  "private": true,
  "version": "1.8.5",
  "description": "BotManager WhatsApp — painel completo + multi-instância",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsx scripts/generate-pwa-icons.ts && tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "postinstall": "cd hotbot && npm ci --omit=dev && cd ../telegram && npm ci --omit=dev && (test -f ../shared/package.json && cd ../shared && npm install --omit=dev || true)",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20.16.0 <21 || >=22.3.0"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/formbody": "^8.0.2",
    "@fastify/multipart": "^9.3.0",
    "@napi-rs/canvas": "^0.1.67",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.2",
    "openai": "^6.10.0",
    "pdf-parse": "^2.4.5",
    "pdfjs-dist": "^4.10.38",
    "pg": "^8.21.0",
    "web-push": "^3.6.7",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "@types/pg": "^8.20.0",
    "@types/web-push": "^3.6.4",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
